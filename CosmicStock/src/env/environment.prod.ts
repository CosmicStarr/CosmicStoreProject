export const environment = {
  production: true,
  // Same-host deploy: the API serves this build from wwwroot, so /api/ is enough.
  // If the API is on another host, set this to https://api.yourdomain.com/api/
  baseUrl: '/api/',
};
